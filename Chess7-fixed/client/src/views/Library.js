export async function Library({api}){const b=await api('/api/library');return{mount(root){root.innerHTML=`<section class="fade"><div class="view-head"><div><h1 class="title">Библиотека книг</h1><p class="sub">Классические источники для тактической тренировки.</p></div></div><div class="grid">${b.map(x=>`<article class="card"><div style="height:110px;border-radius:10px;background:linear-gradient(135deg,#38352f,#191919);display:grid;place-items:center;font-size:34px">♜</div><p><span class="badge">Public Domain*</span></p><h3>${x.title}</h3><p class="sub">${x.author} · ${x.year}</p><p>${x.desc}</p><div class="bar"><i style="width:${Math.min(100,x.tasks*2)}%"></i></div><p class="sub">${x.tasks} заданий</p></article>`).join('')}</div><p class="sub">* Статус public domain должен быть отдельно проверен для конкретной юрисдикции и издания перед публикацией.</p></section>`},unmount(){}}}

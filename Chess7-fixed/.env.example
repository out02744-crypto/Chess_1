# Required in production
JWT_SECRET=replace-with-a-long-random-secret

# Optional. For Railway with a Volume mounted at /data:
# DB_PATH=/data/chesstrainer.db

# Railway sets PORT automatically; local default is 3000.
